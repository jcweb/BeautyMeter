<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:angle="90"
                android:centerColor="@color/color_progress_bg"
                android:endColor="@color/color_progress_bg"
                android:startColor="@color/color_progress_bg" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip
            android:clipOrientation="vertical"
            android:gravity="bottom">
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:centerColor="@color/color_user_ceter_head_bg"
                    android:centerX="0.75"
                    android:endColor="@color/color_user_ceter_head_bg"
                    android:startColor="@color/color_user_ceter_head_bg" />
            </shape>
        </clip>
    </item>

</layer-list>
